apiVersion: v1
kind: Service
metadata:
  name: {{ include "document-db-chart.fullname" . }}
  labels:
    {{- include "document-db-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "document-db-chart.selectorLabels" . | nindent 4 }}
