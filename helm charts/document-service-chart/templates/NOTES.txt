{{/*
  # Author: Abdalrhman Alkrain
  # Year: 2025
  # Version: 1.0
*/}}

Thank you for installing {{ .Chart.Name }}!

Your release "{{ .Release.Name }}" has been successfully deployed.

To access your service:

  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "document-service.chart.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
  kubectl port-forward $POD_NAME 8080:80