# Author: Abdalrhman Alkrain
# Year: 2025
# Version: 1.0

apiVersion: v1
kind: Secret
metadata:
  name:  {{ include "app.name" .}}
  namespace: {{ index .Values.namespace }}
  labels:
    {{ include "common.labels" . | nindent 4 }}
type: Opaque
stringData:
  NEXUS_ADMIN_PASSWORD: {{ .Values.nexus.secret.NEXUS_ADMIN_PASSWORD }}
