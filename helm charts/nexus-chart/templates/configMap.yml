# Author: Abdalrhman Alkrain
# Year: 2025
# Version: 1.0

apiVersion: v1
kind: Configmap
metadata:
  name:  {{ include "app.name" .}}
  namespace: {{ index .Values.namespace }}
  labels:
    {{ include "common.labels" . | nindent 4 }}
data:
  NEXUS_SECURITY_RANDOMPASSWORD: {{ .Values.nexus.configMap.NEXUS_SECURITY_RANDOMPASSWORD }}
  